.data   :

alpha:      .space  80                              # declaring enough space for a 20 integer array
beta:       .space  160                             # Declaring enough space for a 20 double array
charlie:    .word   9, 8, 7, 6, 5, 4, 3, 2, 1, 0    # A pre initialized array of words (ints)
delta:      .word   42:5                            # an array of 5 words (ints) pre initialized to 42 (meaning all of them will be 42 from the get-go)
echo:       .byte   'a', 'b', 'c'                   # An array of bytes (char) pre initialized
